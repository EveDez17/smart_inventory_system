version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    volumes:
      - .:/app
      - static_volume:/app/static
      - media_volume:/app/media
volumes:
  static_volume:
  media_volume:



